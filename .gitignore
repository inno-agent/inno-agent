# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/

# env
.env
.env.*
!.env.example

# local dev certs (mkcert output — never commit)
infrastructure/nginx/certs/*.pem
infrastructure/nginx/certs-dev/*.pem

# dev stack identity key (generated by scripts/dev.sh on the server)
backend/identity/dev-private-key.dev.pem

# Локальные модели ollama (bind-mount, переживают down -v)
.ollama/

.gocache/