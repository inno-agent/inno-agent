name: inno-agent

# ─── Networks ───────────────────────────────────────────────────────────────
networks:
  innoagent:
    name: innoagent

# ─── Volumes ────────────────────────────────────────────────────────────────
volumes:
  postgres-data:
  redis-data:
  authentik-media:
  prometheus-data:
  grafana-data:
  alertmanager-data:

# ─── Secrets ────────────────────────────────────────────────────────────────
secrets:
  identity_rsa_key:
    file: ./backend/identity/dev-private-key.pem

# ─── Authentik shared definition ────────────────────────────────────────────
x-authentik: &authentik
  image: ${AUTHENTIK_IMAGE}
  restart: unless-stopped
  env_file:
    - .env
  environment:
    AUTHENTIK_POSTGRESQL__HOST: postgres
    AUTHENTIK_POSTGRESQL__USER: authentik
    AUTHENTIK_POSTGRESQL__NAME: ${POSTGRES_DB}
    AUTHENTIK_POSTGRESQL__PASSWORD: ${AUTHENTIK_PG_PASSWORD}
    AUTHENTIK_REDIS__HOST: redis
    AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
    AUTHENTIK_DISABLE_UPDATE_CHECK: "true"
    AUTHENTIK_DISABLE_STARTUP_ANALYTICS: "true"
    AUTHENTIK_BOOTSTRAP_PASSWORD: ${AUTHENTIK_ADMIN_PASSWORD}
    AUTHENTIK_BOOTSTRAP_EMAIL: admin@localhost
  volumes:
    - ./infrastructure/authentik/blueprints:/blueprints/custom:ro
    - authentik-media:/media
  healthcheck:
    test: ["CMD", "ak", "healthcheck"]
    interval: 30s
    timeout: 30s
    retries: 10
    start_period: 60s
  networks:
    - innoagent
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy

services:

  # ─── Reverse proxy (nginx) ──────────────────────────────────────────────────
  proxy:
    image: ${NGINX_IMAGE:-nginx:alpine}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:80"
      - "443:443"
    networks:
      - innoagent
    volumes:
      - ./infrastructure/nginx/templates:/etc/nginx/templates:ro
      - ./infrastructure/nginx/certs:/etc/nginx/certs:ro
    depends_on:
      authentik-server:
        condition: service_healthy

  # ─── PostgreSQL ────────────────────────────────────────────────────────────
  postgres:
    image: ${POSTGRES_IMAGE}
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      POSTGRES_USER: ${POSTGRES_ADMIN_USER}
      POSTGRES_DB: ${POSTGRES_DB}
      AUTHENTIK_PG_PASSWORD: ${AUTHENTIK_PG_PASSWORD}
      IDENTITY_PG_PASSWORD: ${IDENTITY_PG_PASSWORD}
      CHAT_PG_PASSWORD: ${CHAT_PG_PASSWORD}
      REVIEW_PG_PASSWORD: ${REVIEW_PG_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d ${POSTGRES_DB} -U ${POSTGRES_ADMIN_USER}"]
      interval: 10s
      timeout: 30s
      retries: 10
      start_period: 20s
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init:/docker-entrypoint-initdb.d:ro
    networks:
      - innoagent

  # ─── Redis (authentik cache/broker) ────────────────────────────────────────
  redis:
    image: ${REDIS_IMAGE:-redis:7-alpine}
    restart: unless-stopped
    command: --save 60 1 --loglevel warning
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    volumes:
      - redis-data:/data
    networks:
      - innoagent

  # ─── Authentik IdP ──────────────────────────────────────────────────────────
  authentik-server:
    <<: *authentik
    command: server

  # ─── Authentik worker (applies blueprints, async tasks) ────────────────────
  authentik-worker:
    <<: *authentik
    command: worker

  # ─── Identity service (JWT issuer + user context) ─────────────────────────
  identity:
    build:
      context: ./backend
      dockerfile: identity/Dockerfile
    restart: unless-stopped
    environment:
      METRICS_ENV: local
      AUTH_JWT_PRIVATE_KEY_PATH: /run/secrets/identity_rsa_key
      AUTH_DATABASE_DSN: postgresql://identity:${IDENTITY_PG_PASSWORD}@postgres:5432/inno_auth?sslmode=disable
      OIDC_ISSUER: "${AUTH_SCHEME:-https}://auth.${AUTH_DOMAIN:-localhost}/application/o/inno-agent/"
      OIDC_CLIENT_ID: "inno-agent-web"
      OIDC_JWKS_URL: "http://authentik-server:9000/application/o/inno-agent/jwks/"
      # Refresh-token TTL for the generic /refresh flow (default 720h).
      AUTH_REFRESH_EXPIRY: ${AUTH_REFRESH_EXPIRY:-720h}
    secrets:
      - identity_rsa_key
    networks:
      - innoagent
    depends_on:
      postgres:
        condition: service_healthy
      authentik-server:
        condition: service_healthy

  # ─── Chat API DB migrations ────────────────────────────────────────────────
  chat-api-migrate:
    image: migrate/migrate:latest
    volumes:
      - ./backend/chat-api/migrations:/migrations:ro
    command:
      - -path=/migrations
      - -database=postgresql://chat:${CHAT_PG_PASSWORD}@postgres:5432/llm_chat?sslmode=disable
      - up
    networks:
      - innoagent
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  # ─── Chat API ──────────────────────────────────────────────────────────────
  chat-api:
    build:
      context: ./backend
      dockerfile: chat-api/Dockerfile
    restart: unless-stopped
    environment:
      METRICS_ENV: local
      DATABASE_URL: postgresql://chat:${CHAT_PG_PASSWORD}@postgres:5432/llm_chat?sslmode=disable
      ORCHESTRATOR_URL: http://orchestrator:8080
      AUTH_SERVICE_URL: http://identity:8081
      SERVER_PORT: "8000"
    networks:
      - innoagent
    depends_on:
      chat-api-migrate:
        condition: service_completed_successfully
      identity:
        condition: service_started
      orchestrator:
        condition: service_started

  # ─── Review API ────────────────────────────────────────────────────────────
  review-api:
    build:
      context: ./backend
      dockerfile: review-api/Dockerfile
    restart: unless-stopped
    ports:
      - "8001:8001"
    environment:
      METRICS_ENV: local
      ORCHESTRATOR_URL: http://orchestrator:8080
      AUTH_SERVICE_URL: ${REVIEW_AUTH_SERVICE_URL-http://identity:8081}
      GITFLAME_BASE_URL: ${GITFLAME_BASE_URL:-}
      GITFLAME_TOKEN: ${GITFLAME_TOKEN:-}
      SERVER_PORT: "8001"
      REVIEW_DATABASE_DSN: postgresql://review:${REVIEW_PG_PASSWORD}@postgres:5432/inno_review?sslmode=disable
      REVIEW_REFRESH_ENC_KEY: ${REVIEW_REFRESH_ENC_KEY:-}
    networks:
      - innoagent
    depends_on:
      postgres:
        condition: service_healthy
      identity:
        condition: service_started
      orchestrator:
        condition: service_started

  # ─── Ollama (LLM runtime) ─────────────────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    networks:
      - innoagent
    volumes:
      # Bind-mount to host so models survive `docker compose down -v`.
      - ./.ollama:/root/.ollama
    environment:
      OLLAMA_HOST: "0.0.0.0"
      OLLAMA_NUM_PARALLEL: ${OLLAMA_NUM_PARALLEL:-6}
      OLLAMA_MAX_LOADED_MODELS: ${OLLAMA_MAX_LOADED_MODELS:-4}
      OLLAMA_MAX_QUEUE: ${OLLAMA_MAX_QUEUE:-2048}
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ─── Pull models on first boot ────────────────────────────────────────────
  ollama-pull:
    image: ollama/ollama:latest
    networks:
      - innoagent
    environment:
      OLLAMA_HOST: http://ollama:11434
      LLM_MODELS: ${LLM_MODELS:-qwen2.5:0.5b llama3.2:1b qwen2.5-coder:1.5b}
      ROUTER_MODEL: ${ROUTER_MODEL:-fauxpaslife/arch-router:1.5b}
    entrypoint: ["sh", "-c", "for m in $$LLM_MODELS $$ROUTER_MODEL; do ollama pull $$m; done"]
    depends_on:
      ollama:
        condition: service_healthy
    restart: "no"

  # ─── AI Orchestrator ──────────────────────────────────────────────────────
  orchestrator:
    build:
      context: ./backend
      dockerfile: innoagent/Dockerfile
    restart: unless-stopped
    networks:
      - innoagent
    environment:
      METRICS_ENV: local
      LLM_BASE_URL: http://ollama:11434/v1
      API_PORT: "8080"
      IDENTITY_URL: http://identity:8081
      LLM_MODELS: ${LLM_MODELS:-qwen2.5:0.5b llama3.2:1b qwen2.5-coder:1.5b}
      ROUTER_MODEL: ${ROUTER_MODEL:-fauxpaslife/arch-router:1.5b}
    depends_on:
      ollama:
        condition: service_healthy
      ollama-pull:
        condition: service_completed_successfully

  # ─── Frontend ─────────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend/chat-front
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - innoagent

  # ─── Redpanda (Kafka-compatible broker) ───────────────────────────────────
  redpanda:
    image: redpandadata/redpanda:v26.1.11
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp=1
      - --memory=1G
      - --reserve-memory=0M
      - --node-id=0
      - --check=false
      - --kafka-addr=0.0.0.0:9092
      - --advertise-kafka-addr=redpanda:9092
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rpk", "cluster", "info"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - innoagent

  # ─── Review Consumer (webhook-driven PR reviewer) ─────────────────────────
  review-consumer:
    build:
      context: ./backend
      dockerfile: review-consumer/Dockerfile
    restart: unless-stopped
    environment:
      METRICS_ENV: local
      KAFKA_BROKERS: redpanda:9092
      KAFKA_TOPIC: gitflame.events
      KAFKA_GROUP: review-consumer
      ORCHESTRATOR_URL: http://orchestrator:8080
      ORCHESTRATOR_TOKEN: ${ORCHESTRATOR_TOKEN:-}
      REVIEW_MODEL: ${REVIEW_MODEL:-qwen2.5-coder:1.5b}
      GITFLAME_BASE_URL: ${GITFLAME_BASE_URL:-}
      GITFLAME_TOKEN: ${GITFLAME_TOKEN:-}
      # Per-assigner delegated auth: read the assigner's encrypted refresh token
      # from inno_review and exchange it via identity's generic /refresh.
      IDENTITY_URL: http://identity:8081
      REVIEW_DATABASE_DSN: postgresql://review:${REVIEW_PG_PASSWORD}@postgres:5432/inno_review?sslmode=disable
      REVIEW_REFRESH_ENC_KEY: ${REVIEW_REFRESH_ENC_KEY:-}
      BOT_GITFLAME_USERNAME: ${BOT_GITFLAME_USERNAME:-}
      ONBOARDING_URL: ${ONBOARDING_URL:-}
    networks:
      - innoagent
    depends_on:
      redpanda:
        condition: service_healthy
      postgres:
        condition: service_healthy
      orchestrator:
        condition: service_started
      identity:
        condition: service_started

  # ─── Review Webhook (Slice 3: webhook → Kafka producer) ──────────────────
  review-webhook:
    build:
      context: ./backend
      dockerfile: review-webhook/Dockerfile
    restart: unless-stopped
    environment:
      METRICS_ENV: local
      SERVER_PORT: "8002"
      KAFKA_BROKERS: redpanda:9092
      KAFKA_TOPIC: gitflame.events
      WEBHOOK_AUTHORIZATION: ${WEBHOOK_AUTHORIZATION:-}
    networks:
      - innoagent
    depends_on:
      redpanda:
        condition: service_healthy

  # ─── Review Frontend ──────────────────────────────────────────────────────
  review-front:
    build:
      context: ./frontend/review-front
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - innoagent

  # ─── Monitoring (Prometheus + Grafana) ─────────────────────────────────────
  alertmanager:
    image: prom/alertmanager:v0.27.0
    restart: unless-stopped
    entrypoint: ["/bin/sh", "/etc/alertmanager/entrypoint.sh"]
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID}
    ports:
      - "9093:9093"
    volumes:
      - ./infrastructure/monitoring/alertmanager:/etc/alertmanager:ro
      - alertmanager-data:/alertmanager/data
    networks:
      - innoagent

  prometheus:
    image: prom/prometheus:v2.55.1
    restart: unless-stopped
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --web.enable-lifecycle
    ports:
      - "9090:9090"
    volumes:
      - ./infrastructure/monitoring/prometheus:/etc/prometheus:ro
      - prometheus-data:/prometheus
    networks:
      - innoagent
    depends_on:
      - alertmanager

  grafana:
    image: grafana/grafana:11.4.0
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ./infrastructure/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infrastructure/monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    networks:
      - innoagent
    depends_on:
      - prometheus

  blackbox-exporter:
    image: prom/blackbox-exporter:v0.25.0
    restart: unless-stopped
    command:
      - --config.file=/etc/blackbox/blackbox.yml
    volumes:
      - ./infrastructure/monitoring/prometheus/blackbox.yml:/etc/blackbox/blackbox.yml:ro
    networks:
      - innoagent

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.16.0
    restart: unless-stopped
    environment:
      DATA_SOURCE_NAME: postgresql://${POSTGRES_ADMIN_USER:-postgres}:${POSTGRES_ADMIN_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-authentik}?sslmode=disable
    networks:
      - innoagent
    depends_on:
      postgres:
        condition: service_healthy
