# OS
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/

# env
.env
.env.*
!.env.example

# local dev certs (mkcert output — never commit)
infrastructure/nginx/certs/*.pem

# Локальные модели ollama (bind-mount, переживают down -v)
.ollama/

# vLLM model cache (bind-mount, переживают down -v)
.vllm-cache/

.gocache/